import { TestBed, inject } from '@angular/core/testing';

//import { LocationStructureService } from './location-structure.service';

describe('LocationStructureService', () => {
  // beforeEach(() => {
  //   TestBed.configureTestingModule({
  //     providers: [LocationStructureService]
  //   });
  // });

  // it('should be created', inject([LocationStructureService], (service: LocationStructureService) => {
  //   expect(service).toBeTruthy();
  // }));
});
